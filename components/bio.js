import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const BioSection = styled(Box)`
  position: relative;
`

export const BioYear = styled.span`
  font-weight: bold;
  margin-right: 1em;
`

export const BioScore = styled.div`
  position: absolute;
  font-weight: 800;
  top: 0px;
  right: 0px;
`
